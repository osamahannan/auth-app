import React from 'react';

const UserProfile: React.FC = () => {
  return (
    <div>
      <h2>User Profile</h2>
      <p>Logged in user details (fetched via shared state).</p>
    </div>
  );
};

export default UserProfile;